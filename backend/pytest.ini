[pytest]
# 注册自定义 mark / Register custom marks
markers =
    integration: 集成测试，需要真实 API 调用 (requires real API calls)

# 测试文件发现路径 / Test file discovery
testpaths = tests

# 默认 asyncio 模式 / Default asyncio mode
asyncio_mode = auto
